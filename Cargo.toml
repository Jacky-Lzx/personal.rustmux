[package]
name = "rustmux"
version = "0.1.0"
edition = "2024"
description = "Rustmux human-reviewed implementation track"
license = "MIT"

[dependencies]
nix = { version = "0.31", features = ["fs", "term", "process", "poll"] }
signal-hook = "0.3"
unicode-width = { version = "0.2.2", default-features = false }
