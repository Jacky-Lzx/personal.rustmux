[package]
name = "rustmux"
version = "0.1.0"
edition = "2024"
description = "Rustmux human-reviewed implementation track"
license = "MIT"

[dependencies]
nix = { version = "0.31", features = ["fs", "term", "process"] }
